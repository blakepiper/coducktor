[package]
name = "coducktor-runners"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
coducktor-contract = { path = "../coducktor-contract" }
coducktor-core = { path = "../coducktor-core" }
coducktor-protocol = { path = "../coducktor-protocol" }
libc.workspace = true
regex.workspace = true
# Only the blocking client (its own internal tokio runtime, no async runtime needed in this
# crate) — the opencode backend's HTTP+SSE transport (B9a.2d).
reqwest = { workspace = true, features = ["blocking"] }
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true

[dev-dependencies]
tempfile = "3"
