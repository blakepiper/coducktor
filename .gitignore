target/
node_modules/
.idea

# Coducktor runtime state in this repo (runs, worktrees, and config).
.ai/coducktor/

# Legacy state is retained only long enough for the startup migration to move it.
.ai/cezar/

# Locally installed agent skills (per-machine installs, not repo content).
.agents/
skills-lock.json

# Local symlink into .agents/skills.
.claude/skills

# Mock-agent output from local dry-run experiments.
/notes.md
